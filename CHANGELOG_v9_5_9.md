# Rayo Admin v9.5.9 — Index Crash Hotfix

## اصلاح انجام‌شده
- حذف `MutationObserver` سراسری نسخه 9.5.8 از ماژول Brand/Access.
- اعمال نام برند فقط در نقاط کنترل‌شده و با اجرای محدود انجام می‌شود.
- جلوگیری از بازنویسی تکرارشونده عنوان صفحه با نگهداری عنوان اصلی صفحه.
- هیچ منطق عملیاتی، داده، CSS، منو، گزارش، حقوق، شیفت، دسترسی کاربر یا Seed تغییر نکرده است.
- Cache/Build به 9.5.9 افزایش یافت.

## علت
در v9.5.8 یک `MutationObserver` روی `document.documentElement` با `subtree:true` نصب شده بود. داشبورد هنگام شروع تعداد زیادی تغییر DOM ایجاد می‌کند؛ Observer برای هر تغییر دوباره `applyBrand()` و چند Query سراسری اجرا می‌کرد. این طراحی می‌توانست بار CPU/Memory شدید و قفل یا Crash مرورگر ایجاد کند.
