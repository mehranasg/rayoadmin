# ChangeLog — Rayo Admin v10.10.1

- جست‌وجوی آیتم منو در نمای رسپی.
- جست‌وجوی متنی در «مغایرت و گردش».
- حذف دکمه‌های تکراری «بروزرسانی» و «ذخیره روی سرور» از محتوای صفحات؛ عملیات تخصصی ذخیره فرم‌ها حفظ شده است.
- اصلاح History جزئیات تأمین‌کننده؛ Back مرورگر ابتدا به فهرست تأمین‌کنندگان برمی‌گردد.
- حذف کلید متنی «مشاهده رمز» در Loginهای دارای آیکون چشم.
- انتخاب کامل مقدار ورودی‌های عددی و مالی با Double Click.
- افزودن تنظیم `settings.featureVisibility` برای پنهان‌کردن امکانات راه‌اندازی‌نشده بدون حذف داده.
- تبدیل «فروش» و «خرید و تأمین» به ورودی مستقیم Sidebar و تب‌های داخل صفحه.
- قرارگرفتن Audit سپیدز در تب‌های صفحه فروش.
- تغییر عنوان Sidebar از «قیمت تمام شده» به «قیمت‌گذاری».
- افزودن ورودی «مدیریت منو» با اتصال موقت به نمای فعلی آیتم‌های منو تا فاز v10.11.0.
- دسترسی «ارزش روز موجودی» و «خسارت، مغایرت و ضایعات» از تب‌های گزارشات؛ منبع داده بدون کپی باقی مانده است.
- حفظ کامل کاتالوگ ۶۱۵ قلمی، ۵۳۶ کد سپیدز، ۱۲ تب ثابت انبار و Guardهای Seed/API.

## تغییر داده

تنها فیلد جدید اختیاری:

```json
{
  "settings": {
    "featureVisibility": {
      "cash": true,
      "sales": true,
      "personnel": true,
      "payroll": true,
      "menu": true,
      "pricing": true,
      "inventory": true,
      "purchase": true,
      "training": true,
      "reports": true,
      "assets": true
    }
  }
}
```

نبودن کلید معادل `true` است. Migration یا ورود مجدد داده لازم نیست.
