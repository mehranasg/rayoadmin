# اصلاح ماژول انبار — v9.4.2

در نسخه 9.4.1 سه تابع `ivRefreshData`، `ivSaveData` و `ivCommit` در زمان مهاجرت API حذف شده بودند، در حالی که UI هنوز آن‌ها را فراخوانی می‌کرد.

در v9.4.2 هر سه تابع بازگردانده شدند و از `RAYO_API_GATEWAY`/`RayoData` فعلی استفاده می‌کنند. همچنین Cache Version صفحات به 9.4.2 تغییر کرد.

هیچ منطق کسب‌وکاری دیگری تغییر نکرده است.
