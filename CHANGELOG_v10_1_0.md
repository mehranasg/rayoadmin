# Rayo Admin v10.1.0 — Final Operational Review

این نسخه بر مبنای v10.0.1 ساخته شده و هدف آن کاهش پیچیدگی عملیاتی، اصلاح Routing، تکمیل پنل پرسنل و تبدیل بخش انبار به یک Workflow قابل اجرا است.

## ناوبری و UX
- گزارش صندوق بلافاصله بعد از حقوق و مزایا قرار گرفت.
- قیمت تمام‌شده بلافاصله بعد از گزارش صندوق قرار گرفت.
- مسیرهای `sepids-audit.html` و `reports.html` در Router با Retry واقعی باز می‌شوند تا قبل از نصب View به داشبورد برنگردند.
- منوهای Location، انتقال، COGS، گردش، دریافت انبار و گزارش مغایرت دارای View/عنوان مستقل هستند.
- تب و دکمه‌های تکراری تأمین‌کنندگان نرمال می‌شوند؛ تب «حساب و سوابق خرید» دقیقاً یک بار نمایش داده می‌شود.
- اکشن‌های بالای لیست تأمین‌کنندگان در یک گروه و از ابتدای ردیف چیده می‌شوند.
- Popup تاریخ جلالی روی موبایل محدود به عرض صفحه شد.
- Login ادمین و پرسنل Show Password دارند.
- Dropdownهای فرم‌های ورودی با placeholder «انتخاب کنید» نرمال می‌شوند؛ فیلترهای «همه» دست‌نخورده باقی می‌مانند.

## بهای تمام‌شده و واحدها
- در رسپی، واحد رسپی و واحد خرید ماده کنار انتخاب ماده و مقدار مصرف نمایش داده می‌شود.
- برچسب‌های عمومی مقدار/تعداد/مبلغ/قیمت/نرخ با واحد صریح اصلاح شدند.
- کوکا، فانتا، اسپرایت، کوکاکولا زیرو و آب معدنی از لیست «نیازمند تغییر قیمت» حذف شدند.

## خرید، تأمین و انبار
- ثبت فاکتور و پرداخت در «خرید و تأمین» باقی می‌ماند و دیگر کاربر را به صفحه انبار نمی‌برد.
- ثبت فاکتور فقط یک رسید `pending` ایجاد می‌کند؛ موجودی فقط پس از تأیید دریافت فیزیکی توسط انباردار افزایش می‌یابد.
- صفحه «دریافت کالا / ورود به انبار» برای تأیید رسیدهای pending اضافه شد.
- صفحه «راهنما و ورک‌فلو مغایرت‌گیری» با مراحل دریافت، انتقال، فروش، ضایعات، شمارش و گزارش اضافه شد.
- گزارش بازه‌ای خسارت و مغایرت به تفکیک کالا و گزارش پرتی/ضایعات به تفکیک پرسنل اضافه شد.
- شمارش تأییدشده می‌تواند مبنای واقعی جدید موجودی باشد؛ Ledger تاریخی حفظ می‌شود.

## پرسنل و پیام‌ها
- دسترسی‌ها از لیست اصلی پرسنل حذف شدند و فقط در «مدیریت دسترسی‌ها» مدیریت می‌شوند.
- پیام روز مدیر اضافه شد و در داشبورد پرسنل نمایش داده می‌شود.
- پیام تاریخ/شیفت در شیفت‌پلن اضافه شد و در همان روز/شیفت در پنل پرسنل دیده می‌شود.
- پروفایل پرسنلی علاوه بر ماه، خلاصه ۱، ۳، ۶ و ۱۲ ماهه دارد.
- ثبت تخلف با یا بدون جریمه مالی اضافه شد. جریمه مالی به رکورد جریمه/تشویق متصل می‌شود.
- واحد مانده طلب مرخصی از تومان به روز اصلاح شد.

## پنل پرسنل
- Back داخلی اضافه شد.
- Dashboard برای تمام بخش‌هایی که کاربر Permission دارد کارت دسترسی سریع می‌سازد.
- شیفت پلن کلی: هفته قبل/بعد و فیلتر سکشن.
- مشاهده پیام مدیر و پیام شیفت.
- مشاهده دریافتی‌ها، مساعده، پاسخ مساعده، مرخصی‌های ثبت‌شده، جریمه‌ها، پاداش‌ها و تخلفات.
- مشاهده انعام ماهانه و وضعیت پرداخت برای نقش‌های مجاز.
- ثبت انعام وزن‌دار فقط برای صندوقدار مجاز.
- ثبت رزرو/بیعانه توسط صندوقدار دارای دسترسی و گزارش مدیریتی رزروها.
- ثبت حادثه اموال با جست‌وجو؛ dropdown فقط اقلام تعدادی را نشان می‌دهد و سایر دارایی‌ها به‌صورت متن قابل ثبت‌اند.
- شمارش موجودی فقط برای کاربر دارای `inventoryCounts` نمایش/اجرا می‌شود.
- آموزش و چک‌لیست‌ها به پایین منوی پنل پرسنل منتقل شدند.
- منوی موبایل با کلیک روی Backdrop بسته می‌شود.

## اموال
- Migration یک‌باره `assets-base-rial-to-toman-v10-1` قیمت‌های مبنا و ارزش‌های مرتبط را از ریال به تومان تقسیم بر ۱۰ می‌کند.
- مبنای ارزش‌گذاری ۱۴۰۵/۰۵/۲۰ و نرخ ۱۹۰٬۰۰۰ تومان حفظ شده است.

## پاک‌سازی فنی
- کارت و متن «قرارداد ذخیره JSON برای React» کاملاً از UI/Source پنل حذف شد؛ مستند فنی جداگانه باقی می‌ماند.
- تزریق دکمه مدیریت دسترسی در لیست پرسنل غیرفعال شد.
- Patchهای قدیمی بی‌اثر حذف شدند.
