# Rayo Admin v9.7.0 — آموزش‌ها، پروتکل‌ها و چک‌لیست‌های عملیاتی

## تغییرات

### پنل مدیریت
- گروه جدید «آموزش و چک‌لیست‌ها» در منوی سمت راست.
- صفحه «آموزش‌ها و پروتکل‌ها»:
  - تعریف گروه‌های دلخواه توسط مدیر.
  - ثبت آموزش / پروتکل / آیین‌نامه.
  - تعیین مخاطب برای همه پرسنل یا یک/چند بخش.
  - فعال/غیرفعال، مشاهده، ویرایش و حذف.
- صفحه «تعریف چک‌لیست‌ها»:
  - چک‌لیست سکشن/مسئول با یک یا چند مسئول.
  - چک‌لیست عمومی فردی برای همه پرسنل فعال.
  - انتخاب شیفت‌های اجرا.
  - تعریف آیتم‌های شروع، حین و پایان شیفت.
- صفحه «گزارش چک‌لیست‌ها»:
  - گزارش روزانه با فیلتر پرسنل، قالب و وضعیت.
  - وضعیت کامل / ناقص / ثبت‌نشده.
  - درصد انجام، توضیح، زمان ثبت و جزئیات آیتم‌ها.
  - موارد مورد انتظار از روی شیفت پلن همان روز محاسبه می‌شوند.

### پنل پرسنل
- منوی «آموزش و پروتکل‌ها» برای مشاهده محتوای مرتبط با بخش پرسنل.
- منوی «چک‌لیست‌های من» برای تکمیل چک‌لیست روزانه در هر شیفت.
- ثبت وضعیت هر آیتم، توضیح کلی، درصد تکمیل و زمان ثبت.

## Data model
همه داده‌ها داخل همان ماژول `personnel` نگهداری می‌شوند و Backend module جدیدی لازم نیست:
- `lists.protocolGroups[]`
- `protocols[]`
- `checklistTemplates[]`
- `checklistRecords[]`

## عدم Regression
- CSS تغییر نکرده است.
- ماژول Backend جدید اضافه نشده است.
- حقوق، جریمه، شیفت، خرید، صندوق، فروش و سایر بخش‌ها تغییر منطقی نداشته‌اند.
