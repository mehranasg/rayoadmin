# Rayo Admin v10.2.1 — Operational Closeout

## خلاصه
این نسخه ادامه v10.1.0 است و روی بستن موارد عملیاتی باقی‌مانده در انبار، موبایل، گزارشات، پرسنل و UX عمومی تمرکز دارد. ساختار اصلی و API موجود حفظ شده است.

## انبار و کنترل مصرف
- منوی سمت راست «انبار و کنترل مصرف» به یک لینک واحد تبدیل شد؛ ناوبری داخلی فقط با تب‌های بالای همان صفحه انجام می‌شود تا دو سیستم Navigation موازی ایجاد نشود.
- تب‌های عملیاتی به فارسی و یکدست شدند: راهنما و ورک‌فلو، موجودی و سفارش، ارزش روز موجودی، دریافت کالا، فروش روزانه، محل‌ها و نگاشت‌ها، انتقال کالا، پرتی و ضایعات، شمارش دوره‌ای، بهای مواد و بستن دوره، روند مغایرت، گزارش خسارت و مغایرت.
- فروش روزانه اسنپ اضافه شد. فروش اسنپ به `inventory.salesPeriods` با `salesSource=SNAPP` ثبت می‌شود و از همان موتور رسپی/Location برای کاهش موجودی استفاده می‌کند.
- URLهای قدیمی فاکتور خرید/پرداخت تأمین‌کننده در Inventory به بخش «خرید و تأمین» Redirect می‌شوند؛ پرداخت تأمین‌کننده دیگر جزو Workflow انبار نیست.
- گزارش «ارزش روز موجودی» اضافه شد: ارزش کل مواد + فهرست مواد از بیشترین ارزش به کمترین.
- شمارش اولیه می‌تواند به‌عنوان Baseline شروع کنترل انتخاب شود و همزمان آخرین قیمت خرید هر ماده ثبت شود.
- تاریخچه قیمت شمارش اولیه در محاسبه ارزش موجودی اولویت صحیح نسبت به Receipt قدیمی دارد.
- Dropdown ماده در «روند مغایرت» اکنون مواد فعال واقعی را لیست می‌کند و «همه مواد» فقط گزینه اول است.
- صفحه راهنما/Workflow شروع کار از یک Baseline واقعی بازنویسی شد.

## گزارشات و حقوق
- «پیش‌بینی هزینه و حقوق» به بخش گزارشات منتقل شد.
- گزارش، حقوق پرداخت‌شده ماه جاری، جمع پرداخت‌های حقوقی ثبت‌شده و پیش‌بینی هزینه حقوق دوره را نمایش می‌دهد.

## پرسنل
- وضعیت «اتمام همکاری» به وضعیت‌های پرسنلی اضافه شد.
- فیلتر وضعیت پرسنل در Viewهای مرتبط اضافه شد و حالت پیش‌فرض «فعال» است؛ امکان «همه» و سایر وضعیت‌ها وجود دارد.
- آموزش‌ها/چک‌لیست‌ها و صفحات حقوقی به‌طور پیش‌فرض فقط نیروی فعال را نمایش می‌دهند.

## گزارش صندوق پنل پرسنل
- ثبت گزارش صندوق برای روز آینده مسدود شد.
- اگر برای تاریخ انتخاب‌شده گزارش صندوق قبلاً ثبت شده باشد، ثبت دوم مسدود می‌شود و پیام می‌دهد برای اصلاح با مدیریت هماهنگ شود.

## UX و موبایل
- Modal/Popup با کلیک روی فضای بیرون بسته نمی‌شود؛ فقط دکمه بستن/لغو آن را می‌بندد.
- Layout موبایل برای Safe Area، Toolbar، عنوان صفحه، Tabهای افقی، فرم‌ها، جدول‌ها، Modalها و Jalali Picker بازبینی شد.
- Modalها در موبایل Full-screen اجباری نیستند و حداکثر ارتفاع کنترل‌شده دارند.
- ورودی‌های پولی به‌صورت سه‌رقم سه‌رقم نمایش داده می‌شوند و هنگام ذخیره به عدد خام تبدیل می‌شوند تا کدهای قدیمی نشکنند.
- Duplicate Buttonهای Import Excel پس از Render پاک‌سازی می‌شوند و Injectionهای قدیمی Recipe/Sales/Purchase Invoice نیز Guard دارند.

## تنظیمات
- بخش «بازنشانی اطلاعات آزمایشی» اضافه شد.
- هر Module مستقل Reset می‌شود و Reset یک بخش سایر Moduleها را تغییر نمی‌دهد.
- Reset دارای دو مرحله تأیید و عبارت `RESET` است.

## سازگاری
- API جدیدی اضافه نشده است.
- فایل‌ها و Moduleهای موجود RayoData حفظ شده‌اند.
