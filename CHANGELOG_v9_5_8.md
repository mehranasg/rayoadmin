# Rayo Admin v9.5.8

- رفع علت اصلی خالی بودن لیست مدیریت دسترسی‌ها: خواندن مستقیم از `state.personnel` به‌جای `window.state`.
- نمایش پیش‌فرض همه پرسنل در مدیریت دسترسی‌ها؛ فیلترهای وضعیت همکاری و دسترسی حفظ شدند.
- اضافه‌شدن دکمه «مدیریت دسترسی‌ها» در هر ردیف لیست پرسنل و اتصال مستقیم به همان Popup مدیریت حساب.
- حذف متن هشدار ورود استاتیک از صفحه ورود مدیریت.
- تغییر عنوان ورود مدیریت به «پنل مدیریت کافه رستوران <نام برند>».
- اضافه‌شدن تنظیم «نام رستوران / برند» در تنظیمات Personnel؛ نام در Sidebar، صفحه ورود مدیریت و پنل پرسنل استفاده می‌شود.
- Cache/Build به 9.5.8 افزایش یافت.
