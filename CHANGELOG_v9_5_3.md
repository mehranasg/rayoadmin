# Rayo Admin v9.5.3 — Personnel Management & Account Access Fix

## تغییرات اصلی

### 1) اصلاح قطعی حساب کاربری پرسنل
- فیلدهای `نام کاربری`، `رمز عبور`، `فعال/غیرفعال بودن پنل` و Permissionها از حالت Injection وابسته به Phase 2 خارج شدند.
- این فیلدها اکنون مستقیماً توسط فرم اصلی Add/Edit پرسنل (`editPersonnelV64`) ساخته می‌شوند.
- ذخیره آن‌ها مستقیماً در مسیر اصلی `savePersonnelV64` انجام می‌شود.
- Username تکراری Validation می‌شود.
- برای حساب فعال، Username و Password الزامی‌اند.
- Permissionهای صندوق، سفارش خرید، فاکتور خرید، انعام و نظرسنجی ذخیره می‌شوند.
- در لیست پرسنل ستون «حساب کاربری» و دکمه مستقیم «حساب کاربری» اضافه شد.
- جست‌وجوی لیست پرسنل Username را نیز پوشش می‌دهد.

### 2) ساختار منوی سمت راست
- گروه جدید «مدیریت پرسنل» با زیرمنوهای:
  - لیست پرسنل
  - پروفایل پرسنلی
  - گزارشات
- «موارد مربوط به شیفت پلن» به «مدیریت شیفت پلن» تغییر نام یافت.
- گزینه «تنظیمات تخلفات» در گروه تنظیمات اضافه شد.

### 3) گزارشات پرسنل
- گزارش کلی و موردی بر اساس بازه تاریخ و پرسنل.
- رتبه‌بندی بیشترین حقوق دریافتی واقعی در بازه بر اساس رکوردهای پرداخت.
- جدول مقایسه پرسنل سالن با امکان مرتب‌سازی بر اساس:
  - انعام
  - جریمه
  - مرخصی
  - تعداد تخلفات کاری
  - دقایق تأخیر
- نمایش KPIهای فردی در حالت انتخاب یک پرسنل.

### 4) تخلفات کاری
- مدل مستقل `workViolations[]` اضافه شد و هیچ اثر مالی خودکار روی Payroll ندارد.
- ثبت/ویرایش/حذف تخلف شامل:
  - تاریخ
  - پرسنل
  - نوع تخلف
  - شرح و مستندات
  - آیا تذکر داده شده است
  - شرح تذکر/اقدام
  - ثبت‌کننده
- تنظیمات تخلفات پرتکرار در `state.lists.violationTypes`.
- سابقه تخلفات قبلی با تغییر تنظیمات حذف یا بازنویسی نمی‌شود.
- آخرین تخلفات در پروفایل پرسنلی نیز نمایش داده می‌شود.

### 5) داشبورد
- KPI «مساعده پرداخت‌شده این ماه» تا تاریخ جاری اضافه شد.
- مبنا فقط رکوردهای نوع `مساعده` در `state.payments` از ابتدای ماه جاری تا امروز است.

## سازگاری
- CSS اصلی پروژه تغییر نکرده است.
- طراحی کلاسیک، RTL، رنگ‌ها و ساختار گرافیکی حفظ شده‌اند.
- API Runtime همچنان فقط `RayoData` است.
- Schema جدید فقط به داده Personnel افزوده شده و با داده‌های قبلی سازگار است.
