# Backend Notes — v10.9.1

## نتیجه

برای این Patch **هیچ تغییر JSON، Module، Endpoint یا Whitelist جدیدی لازم نیست**. داده‌های واقعی قبلی قابل استفاده‌اند و ورود مجدد لازم نیست.

همچنان از Module منطقی `hr` با نام Backend برابر `personnel` و آرایه موجود زیر استفاده می‌شود:

```json
{
  "staffingRequirements": [
    {
      "id": "REQ-...",
      "dayIndex": 0,
      "shift": "صبح",
      "section": "بار",
      "subSection": "",
      "position": "باریستا",
      "requiredCount": 2,
      "active": true
    }
  ]
}
```

ثبت گروهی فقط چند رکورد با همین Schema را Upsert می‌کند. هیچ آرایه یا Object جدیدی ساخته نمی‌شود.

## سازگاری داده قدیمی

- رکوردهای قبلی بدون تغییر Load می‌شوند.
- ترکیب ناسازگار قبلی مانند `بار / صندوقدار` حذف یا تبدیل خودکار نمی‌شود و فقط در UI «نیازمند اصلاح» می‌گیرد.
- مقدار قدیمی `section="پارکینگ"` برای جلوگیری از Migration اجباری حفظ می‌شود؛ Frontend آن را «پوزیشن مستقل: پارک‌بان» نمایش می‌دهد و در نقشه فیزیکی Region نمی‌سازد.
- اگر ذخیره یا GET تأییدی شکست بخورد، تغییر جاری در حافظه Rollback می‌شود و داده Server دست‌نخورده می‌ماند.

## سیاست ایمنی

- Load عادی فقط API است.
- Seed در Runtime عادی خوانده یا Save نمی‌شود.
- این Patch هیچ Auto-save یا Migration نوشتنی ندارد.
- Save فقط پس از Load موفق و تأیید initialized بودن Module مجاز است.
