> **راهنمای عملیات اختیاری کاتالوگ، نه دستور ارتقای خودکار.** فقط برای دادهٔ قدیمی که Preview معتبر در کد جاری دارد؛ این سند باید همراه [مرجع داده](docs/DATA_AND_API_REFERENCE.md) خوانده شود. مقدار ۷۹ در متن آمار Seed تاریخی است، نه تعداد قطعی اقلام محیط شما.

# راهنمای مهاجرت داده واقعی به v10.10.0

## قبل از شروع

فایل‌های فعلی Backend را حذف یا جایگزین نکنید. این نسخه برای استفاده از داده واقعی موجود طراحی شده است.

## مراحل

1. با نسخهٔ جاری و محیط آزمون مستقل، پیش‌نمایش را بررسی کنید؛ انتشار یا اجرای migration در این پالایش انجام نمی‌شود.
2. وارد «تأمین‌کنندگان → تنظیمات» شوید.
3. اگر کارت «مهاجرت یک‌باره به کاتالوگ مرکزی» نمایش داده شد، «پیش‌نمایش مهاجرت» را بزنید.
4. تعداد اقلام جدید، روابط قابل اتصال و موارد تطبیق‌نیافته را بررسی کنید.
5. «پشتیبان‌گیری و اجرای مهاجرت» را بزنید.
6. مرورگر قبل از Save یک JSON شامل نسخه کامل `pricing` و `suppliers` دانلود می‌کند.
7. پس از موفقیت، کارت مهاجرت حذف و پیام «کاتالوگ مرکزی فعال است» نمایش داده می‌شود.

## رفتار ایمن

- کدهای سپیدز تغییر نمی‌کنند.
- `suppliers.items` حذف نمی‌شود و آرشیو سازگاری باقی می‌ماند.
- ۷۹ قلمی که در تأمین‌کنندگان بوده ولی در قیمت‌گذاری نبوده، به کاتالوگ افزوده می‌شود.
- روابط قدیمی با `ingredientId` تکمیل می‌شوند.
- ابتدا Pricing و سپس Suppliers با `verify:true` ذخیره می‌شود.
- اگر Save دوم شکست بخورد، برنامه Rollback جبرانی Pricing را نیز تلاش می‌کند و State داخل پنل را به نسخه قبل برمی‌گرداند.
- هیچ بخش این مهاجرت هنگام Load خودکار اجرا نمی‌شود.

## اگر کارت مهاجرت نمایش داده نشد

یعنی داده قابل انتقالی وجود ندارد یا این مهاجرت قبلاً انجام شده است. مدیریت قلم جدید فقط از `base-data.html` انجام می‌شود.

## ورود داده مجدد

برای این تغییر ورود مجدد داده لازم نیست. فقط اگر موردی در پیش‌نمایش با عنوان «نیازمند بررسی دستی» دیده شود، همان رابطه خاص باید پس از مهاجرت از تب «اقلام هر تأمین‌کننده» اصلاح شود.

