# Rayo Admin v10.3.0 — Final Inventory Consolidation

## دلیل این نسخه
نسخه 10.2.x چند قابلیت را در سورس داشت، اما View انبار توسط چند Wrapper مستقل و زمان‌وابسته ساخته می‌شد. در نتیجه بعضی قابلیت‌ها با توجه به ترتیب Load دیده نمی‌شدند یا هنگام ورود به صفحه مستقل فروش، Sidebar قدیمی دوباره ساخته می‌شد.

## اصلاحات اصلی

### 1) Sidebar واحد در همه صفحات
- منوی `sales-analysis.html` با منبع اصلی Sidebar کاملاً همسان شد.
- «انبار و کنترل مصرف» در Sidebar فقط یک لینک مستقل است و زیرمنوی قدیمی ندارد.
- ورود به فروش دیگر منوی قدیمی انبار را بازنمی‌گرداند.

### 2) View نهایی و قطعی انبار
فایل جدید `js/36-final-inventory-consolidation-v10-3.js` پس از همه Patchهای قبلی Load می‌شود و یک View نهایی با 12 تب می‌سازد:
1. راهنما و ورک‌فلو
2. موجودی و سفارش
3. ارزش روز موجودی
4. دریافت کالا
5. فروش روزانه
6. محل‌ها و نگاشت‌ها
7. انتقال کالا
8. پرتی و ضایعات
9. شمارش دوره‌ای
10. بهای مواد و بستن دوره
11. روند مغایرت
12. گزارش خسارت و مغایرت

هر تب عنوان و توضیح اختصاصی دارد.

### 3) فروش اسنپ
- در تب «فروش روزانه» فرم مستقل فروش اسنپ وجود دارد.
- تاریخ + آیتم‌های منو + تعداد ثبت می‌شود.
- رکورد با `salesSource: SNAPP` در `inventory.salesPeriods` ذخیره می‌شود.
- موتور موجودی همه Sales Periodهای فعال را از طریق رسپی و Location مصرف می‌کند؛ بنابراین فروش اسنپ نیز از موجودی کم می‌شود.
- برای هر تاریخ یک ثبت فعال اسنپ مجاز است و ثبت قبلی قابلیت ابطال دارد.

### 4) شمارش اولیه و آخرین قیمت خرید
- شمارش دوره‌ای همه مواد فعال را نشان می‌دهد؛ وابسته به Track شدن قبلی نیست.
- جست‌وجوی ماده اضافه/حفظ شده است.
- ستون «آخرین قیمت خرید کل بسته (تومان)» کنار موجودی واقعی وجود دارد.
- در حالت «موجودی اولیه / شروع کنترل»، شمارش Baseline می‌شود.
- قیمت واردشده به آخرین قیمت ماده و Price History با Source `stocktake-entry` منتقل می‌شود.

### 5) گزارش‌های انبار و مغایرت
- ارزش روز کل مواد و ارزش هر ماده از بیشترین به کمترین.
- Workflow شروع، روزانه، هفتگی و ماهانه.
- دریافت فیزیکی کالا مستقل از ثبت فاکتور حسابداری.
- انتقال بین Locationها.
- ضایعات و مصرف مجاز با بررسی مدیر.
- COGS استاندارد/واقعی و بستن دوره.
- روند مغایرت با انتخاب واقعی ماده.
- گزارش خسارت مقداری/ریالی و ضایعات به تفکیک پرسنل.

### 6) رفع Race بین Patchهای Inventory
- Wrapper v10.1 یک Marker صریح نصب می‌کند.
- Wrapper v10.2 Flagهای قبلی را حفظ می‌کند و View نهایی v10.3 را Override نمی‌کند.
- Consolidator v10.3 فقط پس از آماده بودن هسته Inventory و Workflow نصب می‌شود.

## سایر قابلیت‌های قبلی
قابلیت‌های v10.2.1 از جمله پیام مدیر، پیام شیفت، تخلف با/بدون جریمه، مساعده، رزرو/بیعانه، فیلتر وضعیت پرسنل، تبدیل یک‌باره ریال اموال به تومان، Reset ماژول‌ها، محدودیت گزارش صندوق، Show Password، کنترل موبایل و سایر موارد حفظ شده‌اند.
