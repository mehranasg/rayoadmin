# Rayo Admin v10.0.1 — Inventory & Waste Control Hotfix

## هدف نسخه
اصلاح Routing ماژول انبار، حذف Duplicate ورود ضایعات Excel، پایدارسازی Load ماژول‌های وابسته و تکمیل کنترل ساده و عملیاتی ضایعات بدون افزایش پیچیدگی عملیات.

## تغییرات

### Routing انبار
- تب‌های `overview / locations / transfer / waste / stock / cost / history` به Router قدیمی و جدید اضافه/همگام شدند.
- `?tab=` در هر Render دوباره خوانده می‌شود تا Wrapperهای قدیمی نتوانند View را به صفحه پیش‌فرض برگردانند.
- Wrapper v10 قبل از Load طولانی داده نصب می‌شود تا صفحه اشتباه هنگام بارگذاری نمایش داده نشود.

### Load و خطای «اطلاعات آماده نیست»
- `supEnsureLoaded`, `pcEnsureLoaded`, `ivEnsureLoaded`, `crEnsureLoaded` در صورت وجود Load همزمان تا پایان همان Load منتظر می‌مانند.
- نبود داده تأمین‌کننده به تنهایی صفحه Inventory را از کار نمی‌اندازد.
- متن خطای قدیمی `اطلاعات انبار، قیمت‌گذاری یا تأمین‌کنندگان آماده نیست` از Runtime JS حذف شده است.
- اگر واقعاً Inventory یا Pricing قابل بارگذاری نباشد، خطای همان منبع نمایش داده می‌شود؛ کل صفحه به خاطر Supplier یا Cash Report متوقف نمی‌شود.

### ضایعات و Import Excel
- در View واقعی ضایعات دقیقاً یک دکمه `ورود ضایعات Excel` وجود دارد.
- پس از Import Excel، Waste Ledger همان لحظه Resync می‌شود؛ Reload صفحه لازم نیست.
- Movementهایی که در Import جایگزین حذف شده‌اند Void می‌شوند تا اثر دوباره روی موجودی نداشته باشند.

### ثبت ضایعات توسط پرسنل
- فرم موبایل پنل پرسنل فقط Location/سکشن مرتبط با کاربر را در اولویت قرار می‌دهد.
- گزارش Waste/Spill/Expiry/Breakage/Missing و مصرف مجاز قابل ثبت است.
- رکورد پرسنل ابتدا `pending / در انتظار بررسی` است و تا تأیید مدیر روی موجودی و KPI اثر ندارد.
- مدیر می‌تواند تأیید، رد، نیازمند پیگیری یا مقدار را اصلاح کند؛ رد/اصلاح مقدار نیازمند توضیح مدیر است.
- گزارش‌دهنده به‌صورت خودکار «مقصر» تلقی نمی‌شود.

### جمع‌بندی پایان شیفت
- پرسنل در پایان شیفت وضعیت `دارای مورد` یا `بدون مورد` را ثبت می‌کند.
- مدیر می‌تواند ثبت‌شده/ثبت‌نشده امروز را برای کاربران برنامه‌ریزی‌شده و مجاز ببیند.
- اگر برنامه شیفت قابل تطبیق نباشد هشدار کاذب تولید نمی‌شود.

### Dashboard ضایعات
- مبلغ ضایعات تأییدشده.
- درصد ضایعات نسبت به فروش.
- درصد ضایعات نسبت به کل مصرف مواد ثبت‌شده.
- درصد ضایعات نسبت به مصرف استاندارد ناشی از فروش.
- تعداد Eventهای Waste.
- مقدار فیزیکی به تفکیک واحد (کیلو/گرم/لیتر/عدد و ...)، بدون جمع‌زدن واحدهای ناسازگار.
- روند ۶ ماهه مبلغ و درصدها.
- مفقودی به‌صورت KPI جدا از Waste.
- تعداد رویدادهای پرسنل در انتظار بررسی.
- تعداد جمع‌بندی پایان شیفت ثبت‌نشده امروز.
- در نبود داده فروش/مصرف، مقدار `—` و وضعیت منبع داده نمایش داده می‌شود؛ کل صفحه متوقف نمی‌شود.

### Data Model
- `inventory.wasteShiftDeclarations[]` اضافه شد.
- Inventory schemaVersion به `2.0.1` ارتقا یافت.
- Data Contract قبلی حفظ شده و Backend module جدیدی برای این نسخه لازم نیست.

## عمداً اضافه نشد
- آپلود عکس/مدرک، چون Endpoint فایل قابل اتکای موجود در معماری فعلی نداریم.
- گردش تأیید چندمرحله‌ای، برای جلوگیری از سنگین‌شدن عملیات.
- ماژول JSON موازی برای Waste؛ داده داخل همان Inventory Source of Truth نگه داشته شده است.
