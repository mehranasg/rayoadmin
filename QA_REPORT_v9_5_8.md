# QA Report — Rayo Admin v9.5.8

## اصلاحات مورد بررسی
- رفع خالی بودن لیست «مدیریت دسترسی‌ها» با اتصال مستقیم به `state.personnel`.
- نمایش پیش‌فرض همه پرسنل و حفظ فیلتر وضعیت همکاری/دسترسی.
- دکمه «مدیریت دسترسی‌ها» در هر ردیف «لیست پرسنل».
- حذف متن هشدار ورود استاتیک از Login مدیریت.
- عنوان Login: `پنل مدیریت کافه رستوران <نام برند>`.
- تنظیم مستقل `restaurantName` در تنظیمات و استفاده در Login، Sidebar و پنل پرسنل.

## تست‌ها
- Runtime unit test: سه پرسنل شامل فعال بدون حساب، غیرفعال و فعال با حساب همگی در مدیریت دسترسی‌ها Render شدند: PASS.
- Runtime unit test: دکمه مدیریت دسترسی در خروجی View لیست پرسنل Inject شد و به `openUserManagementEditorV955` متصل است: PASS.
- تمام فایل‌های `js/*.js` با `node --check`: PASS.
- تمام JSONهای پروژه Parse شدند: PASS.
- 10 صفحه مدیریت Patch برند v9.5.8 را Load می‌کنند: PASS.
- متن «این ورود استاتیک است...» از 10 صفحه مدیریت حذف شده: PASS.
- CSS اصلی نسبت به v9.5.7 بدون تغییر: PASS.
- Cache/Build به 9.5.8 افزایش یافته است.

## نکته Deploy
به علت تغییر Cache Version، کل پوشه نسخه جدید را جایگزین نسخه قبلی کنید. اگر CDN/Proxy Cache دارید، Purge شود.
