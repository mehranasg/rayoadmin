# ChangeLog — Rayo Admin v10.9.1

## مدیریت شیفت‌پلن / نیروی موردنیاز

- وابستگی دقیق پوزیشن به سکشن اضافه شد؛ برای نمونه در سکشن «بار» فقط باریستا و مدیر/سرپرست قابل انتخاب‌اند و صندوقدار فقط در «صندوق» قرار دارد.
- گزینه اولیه همه Dropdownهای فرم ثبت نیاز به «انتخاب کنید» تغییر کرد و روز، شیفت، سکشن، محدوده سالن، پوزیشن و تعداد اعتبارسنجی اجباری دارند.
- کلید «ذخیره و ادامه» اضافه شد؛ پس از تأیید ذخیره Server، Modal باز می‌ماند و فرم برای رکورد بعدی خالی می‌شود.
- «ثبت گروهی» اضافه شد: مدیر روز و شیفت را انتخاب و تعداد هر ترکیب معتبر سکشن/پوزیشن را در یک جدول وارد می‌کند.
- در ثبت گروهی فقط فیلدهای تغییرکرده Upsert می‌شوند؛ مقدار خالی رکورد قبلی را حذف یا صفر نمی‌کند.
- رکوردهای ناسازگار قدیمی بدون Migration و بدون Auto-save حفظ و با برچسب «نیازمند اصلاح» مشخص می‌شوند.
- ذخیره این بخش Snapshot حافظه دارد؛ اگر POST/Verify ناموفق باشد State درون مرورگر به نسخه قبل از عملیات برمی‌گردد.
- ناحیه فیزیکی «پارکینگ» از نقشه کمبود حذف و «پارک‌بان» به‌عنوان پوزیشن مستقل خارج از نقشه نمایش داده شد.

## داده و Backend

- Module یا Endpoint جدید لازم نیست.
- ساختار موجود `personnel.staffingRequirements[]` بدون فیلد اجباری جدید استفاده می‌شود.
- Seed داده‌ای تغییر نکرده و هیچ Migration خودکار یا Save هنگام Load اضافه نشده است.

## Build

- Build: `10.9.1`
- Patch: `js/41-staffing-needs-v10-9-1.js`
- CSS: `css/05-staffing-v10-9-1.css`
- QA: `111/111 PASS`
