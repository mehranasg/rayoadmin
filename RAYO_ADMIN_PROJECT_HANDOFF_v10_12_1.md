# Rayo Admin v10.12.1 — Handoff

مبنای نسخه: v10.12.0.

## معماری صفحات

- `menu-management.html`: آیتم‌های منو، دسته‌بندی‌ها، رسپی‌ها و نسخه‌های تاریخ‌دار رسپی.
- `pricing.html`: خلاصه، نیازمند تغییر، تاریخچه قیمت و تنظیمات قیمت‌گذاری.
- `base-data.html`: تعریف، ورود Excel، ویرایش و مدیریت کاتالوگ مشترک اقلام.

هر سه صفحه از همان Module موجود `pricing` استفاده می‌کنند و داده تکراری ایجاد نمی‌شود.

## مدل قلم

- `category`: گروه گزارش و دسته‌بندی؛ مانند تره‌بار.
- `itemType`: رفتار قلم در رسپی، انبار و تأمین.
- این دو فیلد مستقل‌اند و UI هیچ ترکیبی را مسدود نمی‌کند.

## موجودی

- نمای پیش‌فرض موجودی، جمع کل هر قلم در تمام محل‌هاست.
- نمای تفکیک محل، Dropdown محل و ردیف‌های مکانی را نمایش می‌دهد.
- لیست محل‌ها از تابع Normalizeشده موتور اصلی انبار خوانده می‌شود.
- جست‌وجوی موجودی بدون اجرای `renderView()` فیلتر می‌کند و Focus حفظ می‌شود.

## ایمنی داده

Migration، Seed، Merge یا Save خودکار جدید وجود ندارد. هیچ تغییر Backend لازم نیست.
