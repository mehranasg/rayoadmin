# Rayo Admin v9.5.7 — Access Management Fix

## تغییرات
- عنوان «مدیریت کاربران» به «مدیریت دسترسی‌ها» تغییر کرد.
- گزینه «مدیریت دسترسی‌ها» به‌عنوان زیرمنوی ثابت گروه «مدیریت پرسنل» در Sidebar Runtime باقی می‌ماند.
- صفحه مدیریت دسترسی‌ها همه پرسنل را از `state.personnel` می‌خواند و دیگر به داشتن حساب کاربری یا فعال بودن قبلی پنل وابسته نیست.
- فیلتر وضعیت همکاری اضافه شد و پیش‌فرض روی «فعال» است. با انتخاب «همه پرسنل» یا «غیرفعال» سایر رکوردها نیز نمایش داده می‌شوند.
- فیلتر وضعیت دسترسی پنل اضافه شد: همه / فعال / غیرفعال / حساب تعریف‌نشده.
- اطلاعات پایه مشابه لیست پرسنل (گروه، سکشن/محدوده و پوزیشن) در جدول نمایش داده می‌شود.
- از همان صفحه می‌توان Popup «مدیریت دسترسی» را باز کرد و نام کاربری، رمز عبور، فعال/غیرفعال بودن پنل و Permissionها را تعریف یا ویرایش کرد.
- فعال‌کردن پنل همچنان نیازمند نام کاربری و رمز عبور است.
- شرط Login تغییر نکرده: فقط پرسنل با وضعیت همکاری «فعال» و `userAccess.enabled=true` امکان ورود دارند.
- هیچ تغییر دیگری در طراحی و CSS انجام نشده است.
