# مدل داده کاتالوگ مرکزی اقلام — Rayo Admin v10.10.0

## تصمیم معماری

مرجع اصلی همه اقلام و مواد، آرایه `pricing.ingredients` است. نام فنی آرایه برای سازگاری با داده‌های واقعی، رسپی‌ها، انبار و گزارش‌های قبلی تغییر نکرده؛ اما در UI با عنوان «کاتالوگ اقلام و مواد» نمایش داده می‌شود.

این کاتالوگ در چهار جریان مشترک استفاده می‌شود:

1. رسپی و بهای تمام‌شده
2. انبار، شمارش، گردش و ارزش‌گذاری
3. کنترل مصرف عملیاتی
4. سفارش و ارتباط با تأمین‌کنندگان

## انواع قلم

| کد | عنوان | رسپی/COGS | انبار | تأمین |
|---|---|---:|---:|---:|
| `MENU_INGREDIENT` | ماده اولیه منو | بله | بله | بله |
| `MENU_CONSUMABLE` | ملزومات مستقیم منو | بله | بله | بله |
| `OPERATIONAL_CONSUMABLE` | مصرفی عملیاتی | خیر | بله | بله |
| `GENERAL_INVENTORY` | کالای عمومی / قطعه | خیر | بله | بله |
| `NON_STOCK` | خدمت / هزینه غیرموجودی | خیر | خیر | بله |

مثال «سینگل پیتزا» از نوع `MENU_CONSUMABLE` است؛ می‌تواند با مقدار یک عدد در رسپی هر پیتزا ثبت شود و همزمان موجودی و خرید آن کنترل شود.

## فیلدهای افزوده‌شده به `pricing.ingredients[]`

```json
{
  "id": "ING-6027",
  "code": "6027",
  "name": "سینگل 30 گرد پیتزا",
  "itemType": "MENU_CONSUMABLE",
  "recipeCapable": true,
  "inventoryTracked": true,
  "procurementEnabled": true
}
```

فیلدهای قدیمی مانند `code`، `purchaseUnit`، `recipeUnit`، `packageQuantity`، قیمت و گروه بدون تغییر باقی مانده‌اند.

## ارتباط تأمین‌کننده و قلم

`suppliers.supplierItems[]` اکنون مستقیماً به شناسه کاتالوگ متصل است:

```json
{
  "supplierCode": "SUP-001",
  "ingredientId": "ING-6027",
  "itemCode": "6027",
  "itemCodeSnapshot": "6027",
  "itemNameSnapshot": "سینگل 30 گرد پیتزا",
  "lastPurchasePriceToman": 1025,
  "deliveryTime": "یک روز",
  "isPrimary": "بله"
}
```

`ingredientId` مرجع اصلی است. Snapshot نام و کد برای خوانایی سوابق و بازیابی در شرایط خطا نگهداری می‌شود.

## سازگاری با داده قدیمی

- `suppliers.items` حذف نشده و به‌عنوان Mirror/Archive سازگاری باقی می‌ماند.
- UI دیگر آن را به‌عنوان فهرست مستقل قابل مدیریت نمایش نمی‌دهد.
- هیچ Load عادی، Migration یا Seed به‌طور خودکار چیزی روی سرور ذخیره نمی‌کند.
- مهاجرت داده زنده فقط با دستور صریح مدیر اجرا می‌شود.
- حذف قلمی که در رسپی، تأمین یا انبار سابقه دارد مسدود است؛ چنین قلمی باید غیرفعال شود.

## نیاز Backend

JSON یا Module جدید لازم نیست. Endpointهای موجود `pricing` و `suppliers` کافی‌اند. اگر Backend اعتبارسنجی Strict دارد، باید فیلدهای جدید بالا را بپذیرد؛ در Backendهای Document/JSON فعلی RayoData تغییر Endpoint لازم نیست.

