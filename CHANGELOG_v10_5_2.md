# Rayo Admin v10.5.2 — Bugfix + Snapp Sales Core

## Inventory invariant
- بخش «انبار و کنترل مصرف» همچنان فقط یک لینک مستقیم در Sidebar است و زیرمنو ندارد.
- ۱۲ تب ثابت مستقیماً در `js/14-ops-integration.js` نگهداری می‌شوند.
- فروش اسنپ نیز در Core تب «فروش روزانه» قرار گرفت و برای نمایش آن دیگر به Wrapper نهایی وابسته نیست.

## Bug fixes
- `ingredientUnit` در `js/32-inventory-cost-control-v10.js` در Scope ماژول تعریف و در فرم/گزارش ضایعات استفاده می‌شود.
- Money formatter فیلدهای `data-jalali`, `jalali-input`، عنوان‌های تاریخ/روز/ماه/سال/ساعت و شناسه‌های date/jalali/fromDate/toDate را نادیده می‌گیرد.

## Sales changes
- فروش دستی عادی: مبلغ هر ردیف = تعداد × قیمت فعلی منو و جمع محاسباتی کل نمایش داده می‌شود.
- فروش عادی: دکمه Excel و دانلود نمونه مستقیماً در Core موجود است.
- فروش اسنپ: انتخاب از تاریخ/تا تاریخ، ورود دستی، مبلغ محاسباتی هر ردیف، جمع کل، Excel import و دانلود نمونه.
- Excel اسنپ در صورت داشتن ستون تاریخ، هر روز را به Sales Period مستقل `SNAPP` تبدیل می‌کند.
- رکوردهای SNAPP در `inventory.salesPeriods` ذخیره می‌شوند و بنابراین مصرف رسپی در محاسبات موجودی لحاظ می‌شود.
