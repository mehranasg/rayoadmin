# Rayo Admin — Project Handoff v10.9.1

## 1. نسخه مرجع

نسخه مرجع فعلی `Rayo Admin v10.9.1` است. ادامه پروژه باید از ZIP همین نسخه انجام شود، نه از v10.8.x.

## 2. معماری

- Frontend: HTML، CSS و JavaScript کلاسیک بدون Build Step.
- Backend: API عمومی `RayoData/Load` و `RayoData/Save` با QueryString به نام `module`.
- داده‌ها به تفکیک ۱۰ Module ذخیره می‌شوند: `hr`، `suppliers`، `pricing`، `inventory`، `cashreport`، `assets`، `finance`، `survey`، `errorlog` و `sepidsaudit`.
- نام Backend برای HR برابر `personnel` است؛ بقیه عمدتاً با نام Module یکسان‌اند.
- فایل `js/app-config.js` تنها محل تنظیم Origin و نسخه Build است.
- فایل `js/config.js` Gateway، نرمال‌سازی، Guard ذخیره و سیاست Seed را پیاده می‌کند.

## 3. قانون غیرقابل‌مذاکره ایمنی داده

1. Load عادی فقط Server → Browser است.
2. پاسخ خالی، 404، 500 یا قطع شبکه هرگز «اولین اجرا» محسوب نمی‌شود.
3. Save عادی فقط بعد از Load موفق همان Module در Session و وقتی Module مقداردهی‌شده باشد مجاز است.
4. Seed در Runtime عادی خوانده یا Save نمی‌شود.
5. Seed فقط از `تنظیمات → مدیریت اطلاعات و بکاپ` و با Initialize یا Reset صریح مدیر قابل استفاده است.
6. Reset نیازمند Backup، تأیید اول، عبارت `RESET FROM SEED` و GET تأییدی است.
7. Migrationهای Load فقط در RAM انجام می‌شوند و Auto-save ندارند.

## 4. مسیر مدیریت اطلاعات

```text
personnel.html?view=dataManagement
```

این صفحه شامل Backup کامل، بررسی وضعیت ۱۰ Module، بارگذاری اطلاعات اولیه و بازنشانی کنترل‌شده است.

## 5. انبار

انبار دقیقاً ۱۲ تب اصلی دارد و در Sidebar زیرمنو ندارد:

1. راهنما و ورک‌فلو
2. موجودی و سفارش
3. ارزش روز موجودی
4. دریافت کالا
5. ثبت فروش
6. محل‌ها و نگاشت‌ها
7. انتقال کالا
8. پرتی و ضایعات
9. شمارش دوره‌ای
10. بهای تمام‌شده و بستن دوره
11. مغایرت و گردش
12. گزارش‌ها

سوابق فروش یک زیرتب داخل «ثبت فروش» است و نباید به تب سیزدهم تبدیل شود.

### ارزش‌گذاری

- COGS دوره: میانگین موزون موجودی اول دوره و خریدهای دوره.
- ارزش روز موجودی: آخرین قیمت خرید معتبر.
- این روش فعلاً ثابت است و به همین دلیل Dropdown قابل تغییر ندارد.

### اقلام مصرفی غیرمنویی

قلم در `pricing.ingredients` تعریف می‌شود و `itemType=OPERATIONAL_CONSUMABLE` می‌گیرد. خرید، دریافت، انتقال، شمارش و مصرف آن مانند ماده اولیه است، اما مصرف استاندارد آن از تعداد مهمان، فاکتور، روز کاری، شیفت یا دفعات شست‌وشو به دست می‌آید.

در `inventory.operationalUsagePeriods` مقدار واقعی Driver هر بازه ثبت می‌شود. گزارش تب «گزارش‌ها» مصرف واقعی را با استاندارد مقایسه می‌کند. برای نتیجه معتبر باید شمارش اول و آخر بازه موجود باشد.

## 6. قیمت‌گذاری

- قیمت پیشنهادی حکم اجباری ندارد؛ Modal «اعمال قیمت» عدد نهایی دستی و رند بالا/پایین می‌پذیرد.
- تاریخچه قیمت، قیمت قبل/بعد، Cost، هدف و توضیح مدیر را نگه می‌دارد.
- دسته منو و گروه ماده اولیه در تب تنظیمات قابل افزودن، تغییر نام و حذف‌اند.
- گروه دارای رکورد مصرف‌شده مستقیماً حذف نمی‌شود؛ ابتدا رکوردها باید منتقل شوند.

## 7. تأمین‌کنندگان

- لیست اصلی و اطلاعات مرجع در Module `suppliers` است.
- فاکتور، پرداخت و گردش حساب در Module `inventory` نگهداری می‌شود.
- صفحه پرداخت: `suppliers.html?tab=payment`.
- صفحه حساب: `suppliers.html?tab=accounts`.
- مانده مثبت یعنی رایو به تأمین‌کننده بدهکار است؛ مانده منفی یعنی رایو بستانکار است.
- اطلاعات بانکی در جزئیات تأمین‌کننده بدون ورود به حالت ویرایش قابل مشاهده است.

## 8. گزارش صندوق

گزارش‌ها در `cashreport.reports` هستند. وضعیت مدیریتی جدید با `managementReviewStatus`، یادداشت با `managementNote` و Audit بررسی با `reviewedAt/reviewedBy` ذخیره می‌شود.

اگر رکورد قدیمی این فیلد را نداشته باشد، UI بر اساس `settings.discrepancyLimit` وضعیت نمایشی را محاسبه می‌کند؛ Load آن را خودکار Save نمی‌کند.

## 9. داشبورد

مرکز اقدام جدید چهار مسیر مستقیم دارد:

- اقلام نیازمند سفارش: `inventory.html?tab=overview&orderOnly=1`
- مغایرت‌های شمارش: `inventory.html?tab=history&varianceOnly=1`
- مرخصی‌های جدید: `personnel.html?view=leaveRequests`
- مساعده‌های جدید: `personnel.html?view=advanceRequests`

## 10. پنل پرسنل

- تبدیل بازه هفته به‌صورت مستقل در `js/22-staff-panel.js` انجام می‌شود؛ به توابع صفحه ادمین وابسته نیست.
- `تا تاریخ` در شیفت پلن کلی و شخصی باید شش روز بعد از شروع هفته باشد.
- فایل `css/04-v10-9-fixes.css` فاصله دکمه برگشت و تراز Checkboxها را اصلاح می‌کند.

### مدیریت شیفت‌پلن / نیروی موردنیاز (v10.9.1)

- ثبت نیاز تکی از ماتریس دقیق سکشن–پوزیشن استفاده می‌کند؛ محدودسازی صرفاً بر اساس گروه «سالن» کافی نیست.
- ترکیب‌های استاندارد: بار/باریستا، صندوق/صندوقدار، هاست/هاست، سالن/سالن‌کار و ویتر، قلیان‌خانه/قلیان‌زن، پیک/پیک، پارک‌بان/پارک‌بان، آشپزخانه/پوزیشن‌های آشپزخانه و خدمات/خدماتی.
- همه Dropdownهای ثبت تکی و گروهی با «انتخاب کنید» شروع می‌شوند و قبل از ذخیره اعتبارسنجی اجباری دارند.
- «ذخیره و ادامه» پس از ذخیره تأییدشده فرم را خالی و Modal را باز نگه می‌دارد.
- «ثبت گروهی» یک روز و شیفت می‌گیرد و تمام ترکیب‌های معتبر را ردیفی نمایش می‌دهد؛ فقط ورودی‌های تغییرکرده ذخیره می‌شوند و خانه خالی هیچ رکوردی را حذف نمی‌کند.
- رکوردهای قدیمی ناسازگار خودکار حذف یا اصلاح نمی‌شوند؛ در لیست با برچسب «نیازمند اصلاح» نمایش داده می‌شوند.
- مقدار فنی قدیمی `section="پارکینگ"` برای سازگاری داده حفظ شده، اما در UI به‌عنوان «پوزیشن مستقل: پارک‌بان» نمایش داده می‌شود و ناحیه فیزیکی پارکینگ از نقشه حذف شده است.

## 11. فایل‌های اصلی نسخه

- `js/40-rayo-v10-9-comprehensive.js`: Patch نهایی مسیرها و UI نسخه 10.9.
- `js/41-staffing-needs-v10-9-1.js`: ماتریس سکشن–پوزیشن، ثبت تکی/گروهی و اصلاح نقشه کمبود.
- `css/04-v10-9-fixes.css`: اصلاحات Layout و Print.
- `css/05-staffing-v10-9-1.css`: استایل ثبت گروهی و هشدار ترکیب‌های قدیمی.
- `js/08-rayo-pricing-admin-module.js`: قیمت دستی، دسته‌ها و اقلام غیرمنویی.
- `js/14-ops-integration.js`: ۱۲ تب و ثبت فروش/پرداخت‌های پایه.
- `js/22-staff-panel.js`: پنل پرسنل و اصلاح تاریخ.
- `js/32-inventory-cost-control-v10.js`: Cost Control و توضیح ارزش‌گذاری.
- `js/39-manual-initial-data-v10-8.js`: مدیریت Seed و Backup؛ نام تاریخی فایل تغییر نکرده است.
- `qa_v10_9_1.js`: QA مرجع.

## 12. Backend

Module جدید لازم نیست. جزئیات فیلدهای قبلی در `BACKEND_NOTES_v10_9_0.md` و وضعیت Patch جدید در `BACKEND_NOTES_v10_9_1.md` آمده است.

## 13. Deploy

1. از داده زنده و فایل‌های فعلی Backup بگیرید.
2. ZIP را کامل و با حفظ ساختار استخراج کنید.
3. همه فایل‌ها را روی هاست جایگزین کنید.
4. Hard Refresh انجام دهید.
5. نسخه پایین Sidebar باید `v10.9.1` باشد.
6. Network را بررسی کنید؛ Reload عادی نباید Request به `/seed/` بزند.
7. علاوه بر صفحات قبلی، `personnel.html?view=staffingNeeds` و `personnel.html?view=staffingMap` را Smoke Test کنید.

## 14. QA

```bash
node qa_v10_9_1.js
```

نتیجه مرجع هنگام تحویل: `111/111 PASS`.

## 15. قواعد ادامه توسعه

- تب اصلی جدید برای انبار اضافه نکنید مگر با تصمیم صریح مالک محصول.
- Routeهای ویژه باید URL واقعی و قابل Bookmark داشته باشند.
- افزودن فیلد Schema باید در Gateway، مستند Backend و QA ثبت شود.
- هیچ Patch جدیدی نباید Runtime Load را به Seed یا Auto-save وصل کند.
- دکمه‌های Import/Template قبل از تحویل از نظر تکرار بررسی شوند.
