# Rayo Admin v9.5.6

## اصلاح انجام‌شده

فقط مشکل محل نمایش «مدیریت کاربران» در منوی سمت راست اصلاح شد.

- منبع واقعی Runtime منو در `js/17-v9-3-admin-enhancements.js` اصلاح شد.
- گزینه `مدیریت کاربران` به‌عنوان زیرگزینه مستقیم گروه `مدیریت پرسنل` اضافه شد.
- `userManagement` در تشخیص گروه فعال Sidebar نیز زیر `personnelManagement` قرار گرفت.
- Static fallback منو نیز با همین ساختار هماهنگ شد.
- Cache/Build به `9.5.6` افزایش یافت تا مرورگر فایل JS قدیمی را نگه ندارد.
- هیچ منطق، داده، CSS یا قابلیت دیگری تغییر نکرد.
