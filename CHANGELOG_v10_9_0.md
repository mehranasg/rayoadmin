# ChangeLog — Rayo Admin v10.9.0

## انبار و کنترل مصرف

- عنوان تب «فروش روزانه» به «ثبت فروش» تغییر کرد؛ تعداد تب‌های اصلی همچنان ۱۲ است.
- «ثبت فروش» و «سوابق فروش‌های ثبت‌شده» به دو زیرتب مستقل تقسیم شدند.
- فرم‌های تکراری فروش اسنپ و دکمه‌های تکراری دانلود نمونه حذف می‌شوند.
- لینک داشبورد «سفارش‌گذاری» اکنون مستقیماً صفحه اقلام رسیده به نقطه سفارش را باز می‌کند.
- لینک «مغایرت‌گیری» اکنون فهرست ردیف‌های دارای مغایرت شمارش را باز می‌کند و URL نیز تغییر می‌کند.
- فیلد غیرفعال «روش ارزش‌گذاری» از تنظیمات حذف و با توضیح دقیق جایگزین شد: بهای مصرف دوره با میانگین موزون و ارزش روز با آخرین قیمت معتبر محاسبه می‌شود.
- مدل «قلم مصرفی غیرمنویی» اضافه شد؛ برای اقلامی مانند دستمال، شوینده، دستکش و اسکاچ می‌توان مبنای مصرف و استاندارد تعریف کرد.
- گزارش مقایسه مصرف واقعی/استاندارد اقلام غیرمنویی در تب گزارش‌های انبار اضافه شد.

## بهای تمام‌شده و قیمت‌گذاری

- «اعمال قیمت» اکنون Modal باز می‌کند و مدیر می‌تواند قیمت نهایی را دستی وارد یا رو به بالا/پایین گرد کند.
- دسته‌بندی در فرم افزودن/ویرایش آیتم منو قابل تغییر است.
- مدیریت افزودن، تغییر نام و حذف دسته‌های منو و گروه‌های مواد اولیه در تنظیمات قیمت‌گذاری اضافه شد.
- دکمه‌های تکراری Import Excel و دانلود نمونه حذف می‌شوند.

## تأمین‌کنندگان

- مسیر `suppliers.html?tab=payment` به یک صفحه واقعی ثبت و مشاهده پرداخت تبدیل شد و دیگر تب‌های انبار یا «در حال آماده‌سازی» نشان نمی‌دهد.
- پرداخت می‌تواند به فاکتور مشخص یا به‌صورت کلی ثبت شود و محل پرداخت، روش، پیگیری و شرح دارد.
- در جزئیات تأمین‌کننده، کل خرید، کل پرداخت، مانده بدهکار/بستانکار، اطلاعات تماس، تسویه و تمام اطلاعات بانکی نمایش داده می‌شود.
- از صفحه جزئیات می‌توان مستقیماً به ثبت پرداخت یا ویرایش تأمین‌کننده رفت.

## گزارش صندوق

- وضعیت بررسی مدیریتی شامل «نیازمند بررسی»، «بررسی‌شده» و «بدون نیاز به بررسی» اضافه شد.
- توضیحات مدیریتی، زمان و کاربر بررسی‌کننده ذخیره می‌شوند.
- فیلتر گزارش‌ها بر اساس وضعیت بررسی اضافه شد.
- وضعیت و توضیح مدیریتی در لیست گزارش‌ها نمایش داده می‌شود.
- Modal مشاهده گزارش دکمه پرینت و دکمه ثبت/ویرایش بررسی دارد.

## داشبورد و پرسنل

- مرکز اقدام مدیریت برای سفارش‌گذاری، مغایرت شمارش، درخواست مرخصی و درخواست مساعده اضافه شد.
- محاسبه تاریخ پایان هفته در شیفت پلن کلی و شخصی پنل پرسنل مستقل از توابع صفحه ادمین شد و خطای یکسان‌شدن ابتدا/انتها برطرف شد.
- فاصله دکمه برگشت پنل پرسنل اصلاح شد.
- Checkboxهای دسترسی با متن خود تراز شدند.
- نرخ پایه عملیاتی دستی با جداکننده سه‌رقمی نمایش داده می‌شود.
- فاصله Toolbar/Dropdown با جدول بعدی اصلاح شد.
- نسخه `Rayo Admin v10.9.0` در پایین منوی ادمین نمایش داده می‌شود.

## ایمنی داده

- سیاست v10.8.1 بدون تغییر حفظ شد: Load عادی فقط API، Save فقط پس از Load موفق، Seed فقط Initialize/Reset صریح مدیر.
- هیچ Module جدید Backend اضافه نشده است.
- داده جدید اقلام مصرفی در `pricing` و `inventory` و بررسی صندوق در `cashreport` ذخیره می‌شود.
