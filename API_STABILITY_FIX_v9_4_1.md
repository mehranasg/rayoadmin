# اصلاح پایداری API در v9.4.1

این نسخه دو خطای مستقل فرانت‌اند را اصلاح می‌کند:

1. در ماژول تأمین‌کنندگان، هنگام خطای Load نام ثابت اشتباه `SUPPLIERS_EMPTY_DATA` استفاده شده بود. این باعث می‌شد بعد از خطای اصلی API، `supplierState` همچنان `null` بماند و پیام ثانویه «اطلاعات انبار، قیمت‌گذاری یا تأمین‌کنندگان آماده نیست» ایجاد شود.
2. تمام صفحات، حتی وقتی فقط یک ماژول لازم بود، بخشی از ماژول‌های دیگر را نیز Load می‌کردند. اکنون Loadها بر اساس صفحه انجام می‌شوند و خطاهای تکراری Toast نیز Deduplicate می‌شوند.

## نکته درباره HTTP 500

`HTTP 500 — {"Message":"An error has occurred."}` از خود Backend برمی‌گردد. فرانت‌اند نمی‌تواند بدون ریسک از دست رفتن داده، این خطا را به‌صورت خودکار با Seed بازنویسی کند.

هر ماژول زیر باید در Backend معتبر باشد و فایل داده مربوط به آن برای `RayoData/Load` قابل خواندن باشد:

- personnel
- suppliers
- pricing
- inventory
- cashreport
- assets

الگوی API:

- `GET /api/v1.0/RayoData/Load?module=<name>`
- `POST /api/v1.0/RayoData/Save?module=<name>`

اگر Load برای یک ماژول خاص 500 می‌دهد، ابتدا همان نام ماژول در Backend و فایل/مسیر ذخیره‌سازی آن بررسی شود. نسخه 9.4.1 پیام خطا را با نام دقیق ماژول نمایش می‌دهد تا تشخیص ساده شود.

Seedهای پوشه `seed/` فقط برای Bootstrap/Migration یک‌باره هستند. در این نسخه روی HTTP 500 به‌صورت خودکار Seed روی سرور نوشته نمی‌شود تا در صورت خطای موقت Backend، اطلاعات واقعی بازنویسی نشوند.
