# ChangeLog — Rayo Admin v9.8.0

## اموال و ارزش‌گذاری
- رفع مشکل تبدیل نرخ ارز به `1` هنگام ورود/ذخیره.
- Reset مبنای ارزش‌گذاری به `1405/05/20` با دلار `190,000 تومان`.
- ارزش مبنای هر دارایی از قیمت تاریخی فایل و نسبت `190,000 / 94,000` ساخته می‌شود.
- استهلاک بر اساس **سال کامل سپری‌شده** از تاریخ مبنا است؛ تا `1406/05/19` صفر و از `1406/05/20` یک سال استهلاک اعمال می‌شود.
- نرخ ارز روز و نرخ استهلاک گروه‌ها همچنان قابل تنظیم‌اند.
- افزودن راهنمای استفاده داخل ماژول اموال.
- افزودن `assetIncidents[]` برای گزارش شکستگی، مفقودی، آسیب/خرابی و خروج از رده.
- پرسنل می‌تواند در پنل خودش incident ثبت کند؛ تغییر موجودی فقط پس از بررسی/تأیید مدیر انجام می‌شود.
- لیست incidentها، شرح پرسنل، وضعیت بررسی و نظر مدیر در پنل مدیریت دیده می‌شود.

## درخواست خرید
- سابقه درخواست‌های خرید در پنل خود پرسنل نمایش داده می‌شود.
- در مدیریت `خرید و تامین > درخواست‌های خرید` امکان بررسی و **بستن درخواست** اضافه شد.
- وضعیت `بسته‌شده` به فیلتر مدیریت اضافه شد.

## تأمین‌کنندگان
- حذف رفتار ایجاد چندباره کلید Import Excel تأمین‌کنندگان.
- افزودن روزهای سفارش‌گیری، ساعت/بازه سفارش‌گیری، روزهای ارسال و ساعت/بازه ارسال.
- Import Excel تأمین‌کننده این چهار ستون جدید را نیز پشتیبانی می‌کند.
- صفحه جزئیات مستقل برای هر تأمین‌کننده اضافه شد:
  - اطلاعات اصلی و تماس
  - برنامه سفارش و ارسال
  - اقلام قابل تأمین
  - درخواست‌های خرید
  - فاکتورها
  - پرداخت‌ها
- جزئیات هر درخواست/فاکتور/پرداخت با Popup خوانا نمایش داده می‌شود.
- Permission جدید `suppliersView` اضافه شد.
- کاربر دارای این Permission در پنل پرسنل، لیست تأمین‌کنندگان را به صورت Read-only می‌بیند.
- داشبورد مدیریت بر اساس روز جاری، تأمین‌کنندگانی که همان روز سفارش می‌پذیرند را همراه با زمان سفارش و خلاصه گروه/آیتم نمایش می‌دهد.

## Excel
- کنار ورودی‌های Excel قابل‌مشاهده دکمه «دانلود نمونه» اضافه شد.
- فایل نمونه خام است و فقط Headerهای معتبر همان Import را دارد.
- پوشش: پرسنل، تأمین‌کننده، آیتم منو، مواد اولیه، آخرین قیمت، رسپی، فروش روزانه، ضایعات، فاکتور خرید، اموال و گزارش‌های تحلیل فروش.

## قرارداد JSON / React
- اطلاعات فنی «قرارداد ذخیره JSON برای React» از UI پنل حذف شد.
- مستند فنی مستقل `REACT_JSON_DATA_CONTRACT.md` ایجاد شد.

## Error Log
- ماژول جدید `errorlog` اضافه شد.
- لاگ خطاها در JSON مستقل نگهداری می‌شوند و دیگر قرار نیست با داده‌های عملیاتی ماژول‌های دیگر مخلوط شوند.
- لاگ قدیمی localStorage در اولین بارگذاری به `errorlog` منتقل و سپس پاک می‌شود.
- Seed جدید: `seed/error-log-data.seed.json`.
- ثبت خطا در صفحات مستقل و پنل پرسنل نیز به ماژول `errorlog` متصل شد.
- اگر Backend Allowlist ثابت دارد باید `errorlog` به آن اضافه شود.

## عدم تغییر
- CSS اصلی تغییر نکرده است.
- API ماژول‌های عملیاتی قبلی و جریان‌های Purchase Invoice → Stock Receipt → Ingredient Price History حفظ شده‌اند.
