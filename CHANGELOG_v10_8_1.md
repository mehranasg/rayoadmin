# ChangeLog — Rayo Admin v10.8.1

## هدف نسخه

رفع قطعی مشکل دیده‌نشدن «بارگذاری اطلاعات اولیه» و تبدیل مدیریت داده به یک صفحه مستقل و قابل‌دسترسی، بدون تغییر سیاست ایمنی v10.8.0.

## اصلاحات

### مدیریت اطلاعات و بکاپ

- گزینه جدید «مدیریت اطلاعات و بکاپ» در زیرمنوی تنظیمات اضافه شد.
- مسیر مستقیم: `personnel.html?view=dataManagement`.
- صفحه جدید شامل سه عملیات مشخص است:
  - تهیه بکاپ کامل از ۱۰ ماژول زنده API
  - بارگذاری اطلاعات اولیه برای ماژول مقداردهی‌نشده
  - بازنشانی آگاهانه ماژول مقداردهی‌شده از Seed
- نمایش ابزارها در صفحه قدیمی `personnel.html?view=settings` نیز حفظ شد.
- علت اصلی عدم نمایش، وابستگی تزریق کارت به QueryString ثابت بود؛ تشخیص View تنظیمات مقاوم شد.

### ایمنی Seed و Save

- Load عادی همچنان فقط API را می‌خواند و هیچ Seedی Fetch یا Save نمی‌کند.
- Save عادی همچنان نیازمند Load موفق همان Session و `initialized=true` است.
- Initialize فقط با `INITIALIZE` و برای Module مقداردهی‌نشده مجاز است.
- Reset فقط با Backup قبل از عملیات و عبارت `RESET FROM SEED` مجاز است.
- `saveModule()` برای همه ماژول‌ها پیش از POST یک `serverSaveToken` جدید می‌سازد.
- GET تأییدی باید دقیقاً همان Token را برگرداند؛ صرف برگشت یک JSON معتبر Success قطعی محسوب نمی‌شود.

### Regressionهای درخواستی

- «دسترسی‌های اضافه» در لیست مدیریت دسترسی‌ها نمایش داده نمی‌شود و فقط داخل Modal باقی مانده است.
- مدیر با آیکون چشم رمز کاربران را مشاهده می‌کند.
- Login ادمین و پرسنل آیکون چشم داخل فیلد دارند.
- عنوان KPI: «میانگین رقم هر فاکتور (حدودی)».
- میانگین فاکتور به پایین تا نزدیک‌ترین ۱۰٬۰۰۰ تومان گرد می‌شود.
- داده Excel فروش در `cashreport.salesAnalytics` با `verify:true` ذخیره می‌شود؛ Module جدید لازم نیست.

### انبار

- انبار در Sidebar زیرمنو ندارد.
- دقیقاً ۱۲ تب ثابت حفظ شد.
- نام قطعی سه تب آخر:
  1. بهای تمام‌شده و بستن دوره
  2. مغایرت و گردش
  3. گزارش‌ها

### QA

- Runner جدید و مستقل از مسیر: `qa_v10_8_1.js`.
- فایل تاریخی `qa_v10_5.js` دیگر مبنای QA این نسخه نیست.

## Backend

Module جدید لازم نیست. همان ۱۰ Module فعلی استفاده می‌شوند.

