# Rayo Admin v9.7.1 — Training Routing & Assets Menu Fix

## تغییرات
- ثبت Viewهای `protocols`, `checklistTemplates`, `checklistReports` در Router چندصفحه‌ای؛ لینک‌های «آموزش و چک‌لیست‌ها» اکنون View واقعی مربوط را باز می‌کنند و دیگر به لیست پرسنل برنمی‌گردند.
- «مدیریت اموال و دارایی» از زیرمنوی تنظیمات حذف و به گزینه مستقل Sidebar تبدیل شد.
- همین تغییر منو در صفحه مستقل `sales-analysis.html` نیز اعمال شد تا Sidebar در همه صفحات هماهنگ باشد.
- پنل پرسنل دوباره بررسی شد؛ گزینه‌های «آموزش و پروتکل‌ها» و «چک‌لیست‌های من» در منو و Dashboard وجود دارند و به Viewهای `training` و `checklists` متصل‌اند.
- هیچ Schema، CSS یا منطق سایر ماژول‌ها تغییر نکرد.
