# مستند تحویل — Rayo Admin v10.11.0

## مبنا

این نسخه روی v10.10.1 ساخته شده و تمام اصلاحات ناوبری، نمایش امکانات، کاتالوگ مرکزی و ایمنی داده نسخه‌های قبل را حفظ می‌کند.

## مدیریت منو

مسیر Sidebar: `pricing.html?module=menu`

تب‌ها:

1. آیتم‌های منو
2. دسته‌بندی‌ها
3. رسپی آیتم‌ها
4. نسخه‌های تاریخ‌دار رسپی

تعریف فنی آیتم و رسپی همچنان در `pricing.menuItems[]`، `pricing.recipes[]` و `pricing.recipeVersions[]` باقی مانده است. این انتخاب باعث می‌شود شناسه‌های متصل به فروش و انبار تغییر نکنند.

## قیمت‌گذاری

مسیر Sidebar: `pricing.html`

تب‌ها:

1. خلاصه
2. نیازمند تغییر
3. تنظیمات

تنظیمات قیمت‌گذاری فقط گام گردکردن و Target درصدی دسته‌ها را مدیریت می‌کند. CRUD نام دسته در مدیریت منو قرار گرفته است.

## قواعد داده

- آیتم دارای فروش یا نسخه رسپی حذف نمی‌شود و فقط غیرفعال می‌گردد.
- آیتم غیرفعال در سوابق و محاسبات تاریخی باقی می‌ماند.
- انتخابگر رسپی فقط اقلام Recipe-capable را نشان می‌دهد.
- دسته دارای آیتم قابل حذف نیست.
- تغییر نام دسته به همه آیتم‌های همان دسته و Target قیمت‌گذاری منتقل می‌شود.

## Backend

Module جدید لازم نیست. تغییرات روی همان `pricing` ذخیره می‌شوند. جزئیات در `BACKEND_NOTES_v10_11_0.md` آمده است.

## مرحله بعد

v10.12.0 مربوط به بازطراحی جریان انبار، ورود مستقل، شمارش/Baseline، مغایرت دوره‌ای، گزارش اثر پرتی و جداسازی خرید و تأمین از عملیات انبار است.
