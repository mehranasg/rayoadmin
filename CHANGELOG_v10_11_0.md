# ChangeLog — Rayo Admin v10.11.0

- ایجاد نمای مستقل «مدیریت منو» روی همان JSON فعلی `pricing`.
- تب‌های مدیریت منو: آیتم‌های منو، دسته‌بندی‌ها، رسپی آیتم‌ها، نسخه‌های تاریخ‌دار رسپی.
- محدودکردن نمای «قیمت‌گذاری» به خلاصه، نیازمند تغییر و تنظیمات.
- افزودن ایجاد، تغییر نام و حذف کنترل‌شده دسته‌بندی‌های منو.
- تغییر نام دسته، آیتم‌های وابسته و Target قیمت‌گذاری را نیز به‌روزرسانی می‌کند.
- حذف دسته دارای آیتم ممنوع است.
- آیتم‌های منو به‌صورت پیش‌فرض با وضعیت فعال نمایش داده می‌شوند؛ فیلتر همه/غیرفعال همچنان در دسترس است.
- حذف آیتم دارای فروش یا نسخه تاریخ‌دار رسپی ممنوع است؛ آیتم و نمایش سایت غیرفعال می‌شوند.
- رسپی و سوابق آیتم غیرفعال حذف نمی‌شوند.
- انتقال کلید افزودن ماده رسپی به زیر انتخابگر آیتم منو.
- جست‌وجوی قلم داخل Modal افزودن/ویرایش رسپی.
- محدودکردن انتخاب رسپی به `MENU_INGREDIENT` و `MENU_CONSUMABLE`.
- اقلام مصرفی عملیاتی، ظروف کم‌ارزش، قطعات و خدمات در رسپی نمایش داده نمی‌شوند.
- حفظ کاتالوگ مرکزی، ۵۳۶ کد سپیدز، ساختار JSON و Guardهای Seed/API.

## تغییر JSON

Module یا آرایه جدیدی اضافه نشده است. فقط در صورت غیرفعال‌شدن آیتم دارای سابقه، Metadata زیر به همان `pricing.menuItems[]` افزوده می‌شود:

```json
{
  "status": "غیرفعال",
  "siteStatus": "غیرفعال",
  "archivedAt": "ISO-8601",
  "archivedBy": "admin",
  "archiveReason": "دارای سابقه فروش یا نسخه رسپی"
}
```

Migration و ورود مجدد داده لازم نیست.
