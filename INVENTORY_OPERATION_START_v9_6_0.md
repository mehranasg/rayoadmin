# شروع عملیاتی موجودی مواد اولیه — رایو

## نقطه شروع
یک تاریخ قطع (Cut-off) مشخص انتخاب کنید. موجودی فیزیکی ابتدای کار باید مربوط به پایان/شروع همان تاریخ باشد. خرید یا فروشی که داخل این شمارش اولیه لحاظ شده نباید دوباره به‌عنوان ورود/مصرف بعد از تاریخ مبنا ثبت شود.

## 1) کنترل اطلاعات پایه
قبل از شروع، در قیمت‌گذاری این سه مورد را کنترل کنید:
- مواد اولیه و واحد مصرف
- آیتم‌های منو و کد سپیدز
- رسپی و مقدار مصرف هر ماده در هر آیتم

اگر رسپی یا تبدیل واحد اشتباه باشد، موجودی نظری و مغایرت هم اشتباه خواهد بود.

## 2) ثبت موجودی اولیه
مسیر: `موجودی و انبار ← موجودی و سفارش`

برای هر ماده از «تنظیم موجودی/سفارش» این موارد را ثبت کنید:
- موجودی اولیه
- تاریخ موجودی اولیه
- نقطه سفارش (در صورت نیاز)
- موجودی هدف
- اهمیت مغایرت‌گیری
- تأمین‌کننده اصلی

بهتر است همه مواد یک تاریخ شروع مشترک داشته باشند. اگر ورود 536 ماده در روز اول عملی نیست، ابتدا مواد گران، حساس و پرتکرار را عملیاتی کنید و بقیه را مرحله‌ای اضافه کنید.

## 3) ثبت خریدها از تاریخ شروع
هر فاکتور خرید بعد از تاریخ مبنا باید ثبت شود. ثبت فاکتور خرید:
- ورود انبار ایجاد می‌کند.
- قیمت آخر ماده را به‌روزرسانی می‌کند.
- بدهی/تسویه تأمین‌کننده را قابل پیگیری می‌کند.

صندوقداری که Permission «فاکتور خرید» دارد می‌تواند از پنل پرسنل فاکتور را ثبت کند.

## 4) ثبت فروش هر شب
مسیر: `فروش ← ورود فروش سپیدز` یا `موجودی ← فروش سپیدز`

هر شب ریز فروش همان روز را وارد کنید. برای مغایرت موجودی مهم‌ترین داده، کد آیتم و تعداد فروش است. سیستم با استفاده از رسپی، مصرف نظری مواد اولیه را از فروش محاسبه می‌کند.

Importer v9.6.0 برای یک `تاریخ + آیتم` فقط رکوردی را که قبلاً وجود ندارد اضافه می‌کند و داده قبلی را حفظ می‌کند.

## 5) ثبت پرتی و ضایعات
ضایعات را همان روز ثبت کنید؛ چه ضایعات ماده اولیه باشد و چه آیتم منو. این مقدار جدا از مصرف ناشی از فروش از موجودی نظری کسر می‌شود.

## 6) موجودی نظری
منطق سیستم:

`موجودی نظری = موجودی مبنا + خریدها - مصرف ناشی از فروش - ضایعات ثبت‌شده`

موجودی مبنا آخرین شمارش فیزیکی قبلی است؛ اگر شمارشی وجود نداشته باشد، موجودی اولیه استفاده می‌شود.

## 7) انبارگردانی و مغایرت
مسیر: `موجودی و انبار ← انبارگردانی`

موجودی واقعی را وارد کنید. سیستم اختلاف موجودی واقعی با موجودی نظری را ذخیره می‌کند.

پیشنهاد عملی برای رایو:
- مواد حساس/گران مثل گوشت، مرغ، روغن، پنیر و اقلام با احتمال پرت: روزانه یا چند بار در هفته.
- مواد متوسط: هفتگی.
- مواد کم‌ریسک: دوره‌ای.

## 8) ترتیب روزانه پیشنهادی
1. فاکتورهای خرید همان روز ثبت شوند.
2. ضایعات همان روز ثبت شوند.
3. بعد از بسته‌شدن فروش، ریز فروش سپیدز وارد شود.
4. برای مواد حساس شمارش فیزیکی انجام شود.
5. مغایرت‌های غیرعادی بررسی شوند.

## خطاهایی که باید از آن‌ها جلوگیری شود
- ثبت خریدی که قبل از موجودی اولیه داخل شمارش بوده، به‌عنوان ورود جدید.
- Import دوباره فروش یک تاریخ و شمارش دوباره مصرف.
- استفاده از رسپی یا واحد تبدیل اشتباه.
- ثبت دیرهنگام ضایعات بدون تاریخ درست.
- شروع مواد مختلف با تاریخ‌های مبنای نامشخص بدون مستندسازی.
