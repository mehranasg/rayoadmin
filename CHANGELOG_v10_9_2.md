# تغییرات Rayo Admin v10.9.2

تاریخ انتشار: ۱۴۰۵/۰۵/۲۹ — 2026-08-20

## گزارش صندوق

- دکمه «حذف» به فهرست گزارش‌های روزانه بازگردانده شد.
- حذف فقط در نشست مدیر و پس از ثبت دلیل و تایپ کلمه دقیق «حذف» انجام می‌شود.
- قبل از Save، گزارش از State حذف می‌شود و عملیات با `verify:true` روی سرور تأیید می‌شود.
- در صورت خطای Save، گزارش و تاریخچه تغییرات در RAM به وضعیت قبل برمی‌گردد.
- تاریخ، صندوقدار، فروش محاسباتی، دلیل و مدیر تأییدکننده در `cashreport.changeLog` باقی می‌ماند.

## دسترسی و فرم رمز

- محل آیکون چشم و Padding فیلد رمز در ورود ادمین، ورود پرسنل و مدیریت دسترسی اصلاح شد؛ متن و Cursor دیگر زیر آیکون قرار نمی‌گیرند.
- Checkboxهای دسترسی با Label خود به‌صورت Flex و با تراز عمودی یکسان نمایش داده می‌شوند.

## اقلام تأمین‌کنندگان

- هر ۵۳۶ ماده قیمت‌گذاری با کد اصلی سپیدز وارد Seed تأمین‌کنندگان شد.
- ۲۶ قلم هم‌نام با اطلاعات عملیاتی قبلی Merge شد.
- ۷۹ قلمی که در قیمت‌گذاری وجود نداشت، بدون حذف باقی ماند.
- فهرست نهایی Seed تأمین‌کنندگان ۶۱۵ قلم با ۶۱۵ کد یکتا است.
- برای داده زنده، در «تأمین‌کنندگان ← تنظیمات» عملیات صریح «بررسی و همگام‌سازی اقلام» اضافه شد. این عملیات خودکار نیست، قبل از Save پیش‌نمایش می‌دهد و JSON فعلی را Backup می‌گیرد.

## خرید و تأمین

- تب «میز سفارش‌گذاری» اضافه شد: موجودی لحظه‌ای، نقطه سفارش، موجودی هدف، مسئول سفارش، تأمین‌کننده اصلی و جایگزین و آخرین قیمت را کنار هم نشان می‌دهد.
- قلمی که به نقطه سفارش رسیده ولی سفارش باز ندارد، با وضعیت «ثبت نشده» مشخص می‌شود.
- ثبت سفارش شامل مقدار، قیمت توافقی، مسئول، شماره پیگیری و تاریخ تحویل مورد انتظار است.
- تب «سفارش‌های ثبت‌شده» برای پیگیری وضعیت تا تحویل اضافه شد.
- وضعیت تحویل سفارش عمداً موجودی را خودکار تغییر نمی‌دهد؛ دریافت واقعی و فاکتور فقط از مسیر انبار ثبت می‌شوند تا ثبت تکراری ایجاد نشود.
- تب واقعی «حساب و بدهی‌ها» اضافه شد؛ مانده تأمین‌کنندگان را از بیشترین بدهی مرتب و امکان رفتن مستقیم به ثبت پرداخت را فراهم می‌کند.
- ارزش موجودی هم با میانگین موزون دریافت‌های تأییدشده و هم با آخرین قیمت معتبر نمایش داده می‌شود.
- روزهای سفارش‌گیری، روزهای ارسال، زمان معمول تحویل و مسئول سفارش به فرم تأمین‌کننده اضافه شد.
- مسئول/بخش سفارش‌گذار به فرم هر قلم اضافه شد.

## ایمنی داده

- هیچ Seed یا Merge جدیدی در مسیر Load عادی اجرا یا Save نمی‌شود.
- همگام‌سازی اقلام فقط با اقدام صریح مدیر انجام می‌شود و در صورت خطا State قبلی برمی‌گردد.
- ماژول جدید Backend اضافه نشده است؛ سفارش‌ها در همان `suppliers.purchaseRequests` ذخیره می‌شوند.

