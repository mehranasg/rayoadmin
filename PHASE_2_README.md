# Rayo Admin v9.5.4 — فاز دوم توسعه / Personnel, Seed & Sales Analytics

این نسخه ادامه فاز دوم v9.5 است و بدون تغییر در طراحی کلاسیک، منوی سمت راست، تاریخ شمسی و معماری چندصفحه‌ای، ایرادهای Runtime و یکپارچگی داده فاز دوم را اصلاح می‌کند.

## API
تنها API معتبر در Runtime:

- `GET  {API_ORIGIN}/api/v1.0/RayoData/Load?module=<module>`
- `POST {API_ORIGIN}/api/v1.0/RayoData/Save?module=<module>`

دامنه فقط در `js/app-config.js` قرار دارد.

ماژول‌های Backend مورد استفاده:

- `personnel`
- `suppliers`
- `pricing`
- `inventory`
- `cashreport`
- `assets`
- `finance` **جدید**
- `survey` **جدید**

اگر Backend فهرست مجاز ماژول دارد، دو ماژول `finance` و `survey` نیز باید به آن اضافه شوند.

## فایل JSON هر بخش
هر بخش مستقل است. داده عملیاتی از فایل JSON روی هاست خوانده نمی‌شود؛ `seed/*.json` فقط برای Bootstrap/Migration یک‌باره است.

## قابلیت‌های فاز دوم

### ورود Excel
ورود Excel/CSV برای داده‌های پرتکرار:
- پرسنل
- تأمین‌کنندگان
- آیتم‌های منو
- مواد اولیه و آخرین قیمت‌ها (قابلیت قبلی حفظ شده)
- رسپی
- فروش روزانه سپیدز
- پرتی و ضایعات
- فاکتورهای خرید
- اموال

قالب‌ها در `EXCEL_IMPORT_FORMATS.md` مستند شده‌اند.

### سود و زیان ماهانه
صفحه: `finance.html`

منابع خودکار:
- فروش از گزارش صندوق
- خرید از فاکتورهای خرید
- حقوق و پاداش از ماژول پرسنل

امکانات:
- جایگزینی دستی فروش، خرید، حقوق و پاداش در همان ماه بدون تغییر منبع اصلی
- ثبت سایر درآمدها و هزینه‌ها
- دسته‌بندی هزینه‌های جاری
- VAT پیش‌فرض 10٪ فروش
- مالیات عملکرد پیش‌فرض 5٪ فروش
- سود/زیان تقریبی و حاشیه سود
- محاسبه‌گر سناریویی سود و فروش سربه‌سر

این گزارش مدیریتی و تقریبی است و جایگزین حسابداری رسمی نیست.

### حساب کاربری پرسنل
نام کاربری و رمز عبور دیگر داخل فرم «افزودن/ویرایش پرسنل» قرار ندارند. در «لیست پرسنل» برای هر نفر دکمه مستقل «نام کاربری / رمز» وجود دارد و Popup جداگانه این موارد را مدیریت می‌کند:
- نام کاربری
- رمز عبور
- فعال/غیرفعال بودن دسترسی پنل
- Permissionهای ویژه

دسترسی‌های قابل اعطا:
- گزارش صندوق
- سفارش خرید
- فاکتور خرید
- انعام
- نظرسنجی مشتری

ورود Staff Panel فقط وقتی مجاز است که هم `status === "فعال"` باشد و هم `userAccess.enabled === true`.

### پنل پرسنل
- `staff-login.html`
- `staff-panel.html`

امکانات پایه همه کاربران:
- شیفت پلن کلی
- شیفت‌های خود کاربر
- ثبت درخواست مرخصی
- مشاهده وضعیت درخواست و توضیح مدیر

مدیر در بخش مرخصی پنل مدیریت، درخواست را تأیید/رد و توضیح ثبت می‌کند. درخواست تأییدشده به سوابق مرخصی واقعی نیز منتقل می‌شود.

### دسترسی‌های ویژه پرسنل
با Permission:
- ثبت گزارش صندوق
- ثبت سفارش خرید
- ثبت فاکتور خرید + ورود انبار + بروزرسانی قیمت ماده
- ثبت انعام
- ثبت نظرسنجی مشتری

### نظرسنجی مشتری
صفحه مدیریت: `survey.html`

ثبت توسط کاربر مجاز:
- نام مشتری
- تلفن
- شماره فاکتور
- اقلام فاکتور
- سالن / حضوری / بیرون‌بر / اسنپ
- رضایت کلی
- نظر مشتری

مدیریت:
- طبقه‌بندی راضی/ناراضی/خنثی
- انتخاب علت اصلی
- یادداشت مدیر
- مدیریت فهرست علت‌ها
- نمودار روند ماهانه با تعداد و درصد راضی/ناراضی
- بیشترین علت‌ها
- خروجی Excel/CSV شماره تلفن‌های یکتا

## نکته امنیتی مهم
احراز هویت پنل پرسنل در این نسخه، متناسب با معماری فعلی JSON/API، سمت مرورگر انجام می‌شود و رمز در JSON پرسنل ذخیره می‌شود. برای نسخه‌ای که اطلاعات حساس یا دسترسی اینترنتی گسترده دارد، باید در Backend احراز هویت واقعی، Password Hash و Token/Session سروری پیاده‌سازی شود. این موضوع در مهاجرت آینده به دیتابیس/React باید اولویت داشته باشد.


## اصلاحات QA نسخه 9.5.2

- مسیر واقعی ذخیره فرم پرسنل اصلاح شد؛ دکمه موجود فرم تعریف/ویرایش پرسنل اکنون نام کاربری، رمز عبور، فعال/غیرفعال بودن پنل و Permissionها را همراه همان رکورد پرسنل Persist می‌کند.
- Username تکراری کنترل می‌شود و برای حساب فعال، نام کاربری و رمز عبور الزامی است.
- Import پرسنل از Excel/CSV می‌تواند اطلاعات حساب کاربری و Permissionهای ویژه را نیز وارد کند.
- ورود مجدد همان فایل فروش روزانه یا پرتی/ضایعات به‌صورت جایگزینی همان Import انجام می‌شود تا مصرف/ضایعات دوبرابر نشود.
- فاکتور خرید دارای وضعیت `تسویه‌نشده / تسویه جزئی / تسویه‌شده` و «محل پرداخت» شد.
- محل‌های پرداخت در صفحه موجود «تنظیمات و فهرست‌ها» مدیریت و در `inventory.settings.paymentLocations` با ID پایدار ذخیره می‌شوند؛ ساختار ناوبری جدیدی ایجاد نشده است.
- پرداخت مستقل تأمین‌کننده نیز محل پرداخت را ثبت می‌کند و وضعیت فاکتور لینک‌شده بروزرسانی می‌شود.
- فرم فاکتور در پنل پرسنل نیز وضعیت تسویه و محل پرداخت را رعایت می‌کند.
- Import فاکتور خرید ستون‌های `وضعیت تسویه` و `محل پرداخت` را می‌پذیرد؛ برای فاکتور دارای پرداخت، محل پرداخت معتبر الزامی است. جریان ورود انبار و تاریخچه قیمت ماده حفظ می‌شود.
- لینک مستقیم تب‌های `monthly / entries / calculator / settings` در Finance اصلاح شد.
- `seed/manifest.json` با ۸ ماژول Runtime و Build 9.5.2 همگام شد.

### محل تنظیم محل‌های پرداخت

پنل مدیریت → پرسنل → تنظیمات و فهرست‌ها → کارت «محل‌های پرداخت».

موارد پیش‌فرض:

- حساب جاری پارسیان
- حساب پارسیان کوتاه‌مدت / تنخواه
- صندوق نقدی
- تنخواه صندوقدار
- کارت/حساب دیگر

این فهرست قابل ویرایش است و در کد عملیاتی فاکتور Hard-code نمی‌شود. حذف یک مورد از لیست تنظیمات، آن را برای ثبت‌های جدید غیرفعال می‌کند و نام Snapshot ثبت‌های قبلی حفظ می‌شود.

### نکته Import مجدد فروش و ضایعات

Importer برای فایل فروش و ضایعات یک Fingerprint از فایل می‌سازد. اگر همان فایل بدون تغییر دوباره وارد شود، رکوردهای Import قبلی همان فایل جایگزین می‌شوند و Duplicate ایجاد نمی‌شود. اگر محتوای فایل عوض شود، به‌عنوان Import جدید شناخته می‌شود.

## اصلاحات نسخه 9.5.3 — مدیریت پرسنل

- نسخه 9.5.3 حساب کاربری را در فرم اصلی قرار داده بود؛ این رفتار در 9.5.4 طبق درخواست جدید حذف شد.
- در 9.5.4 حساب کاربری فقط از Popup مستقل «نام کاربری / رمز» در لیست پرسنل مدیریت می‌شود.
- منوی جدید «مدیریت پرسنل» شامل لیست پرسنل، پروفایل پرسنلی و گزارشات است.
- عنوان گروه شیفت به «مدیریت شیفت پلن» تغییر یافته است.
- گزارشات پرسنل با بازه تاریخ، گزارش کلی/فردی و رتبه‌بندی حقوق، انعام، جریمه، مرخصی، تخلف و تأخیر اضافه شده است.
- ثبت مستقل تخلفات کاری و تنظیمات تخلفات پرتکرار اضافه شده است؛ تخلف الزاماً جریمه مالی نیست.
- KPI مساعده پرداخت‌شده ماه جاری به داشبورد اضافه شده است.
- CSS اصلی و طراحی کلاسیک پروژه تغییر نکرده است.


## اصلاحات نسخه 9.5.4 — حساب کاربری، مرخصی، دیتای سپیدز و تحلیل فروش

- Username/Password/Permissions از فرم افزودن و ویرایش پرسنل حذف شدند و فقط از Popup مستقل در لیست پرسنل مدیریت می‌شوند.
- Fallback قدیمی Phase 2 که می‌توانست فیلد حساب کاربری را دوباره داخل فرم Inject کند غیرفعال شد.
- Login پرسنل فقط برای رکورد با وضعیت همکاری دقیقاً `فعال` و دسترسی پنل فعال مجاز است؛ Session کاربر غیرفعال نیز در ورود مجدد پنل باطل می‌شود.
- «درخواست‌های مرخصی» به زیرمنوی مستقل «مدیریت پرسنل» منتقل شد. رد درخواست نیازمند علت است و همان `managerNote` در پنل پرسنل نمایش داده می‌شود. تایید درخواست همچنان یک رکورد واقعی مرخصی ایجاد می‌کند.
- ترتیب «مدیریت شیفت پلن» با «برنامه هفتگی» و سپس «برنامه ماهانه» آغاز می‌شود.
- Seed قیمت‌گذاری با فایل‌های جدید `انواع کالای خام.xlsx` و `آیتمهای منو.xlsx` بروزرسانی شد؛ قیمت منبع ریال است و برای داده عملیاتی به تومان تقسیم بر 10 می‌شود. تاریخ‌های شمسی فشرده مثل `14050401` پشتیبانی می‌شوند.
- Seed فعلی شامل 536 ماده اولیه، 379 آیتم منو، 912 ردیف رسپی و 531 رکورد تاریخچه قیمت ماده است. فایل رسپی جدید در ضمیمه این درخواست موجود نبود؛ 912 ردیف رسپی معتبر نسخه قبلی حفظ شدند و Migration جدید `recipeSeedWins=false` دارد تا رسپی‌های ویرایش‌شده روی سرور بازنویسی نشوند.
- CRUD دستی آیتم منو، ماده اولیه و خطوط رسپی حفظ شده است؛ واحد، وضعیت سایت، قیمت، مقدار بسته و میزان مصرف رسپی قابل ویرایش‌اند.
- بخش «گزارشات مدیریتی» تحلیل فروش بر مبنای تعداد فیش دریافت کرده است: روزانه، ماهانه و سابقه فروش هر کالا.
- Import گزارش فروش به تفکیک تاریخ/ماه فقط تاریخ/ماه‌های موجودنشده را اضافه می‌کند. کلید Duplicate برای گزارش کالا `menuItemId + date` است.
- نمودارها بر اساس تعداد کل فیش، حضوری، سالن و مشترکین هستند؛ مبلغ ریالی فقط به عنوان داده منبع نگهداری می‌شود و معیار رشد/افت نیست.
- در صورت وجود داده یک ماه در چند سال، مقایسه سال‌به‌سال و درصد رشد/افت همان ماه نمایش داده می‌شود.
