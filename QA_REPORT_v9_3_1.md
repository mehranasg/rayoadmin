# QA — v9.3.1

اصلاحات این نسخه محدود به موارد درخواستی است:

1. **ذخیره**: موفقیت POST فوراً موفق محسوب می‌شود؛ Verify در پس‌زمینه است و پیام عدم تطابق به کاربر نشان داده نمی‌شود.
2. **Login صفحات داخلی**: فرم Login در `personnel.html`, `suppliers.html`, `pricing.html`, `inventory.html`, `reports.html`, `cash-report-admin.html`, `assets.html` از اولین Paint مخفی است. `index.html` بدون تغییر، Login را نمایش می‌دهد.
3. **Null-safe render**: قبل از آماده شدن HR، `renderView` فقط Loading نشان می‌دهد و `personnel` و `payrollClosures` را به آرایه امن تبدیل می‌کند.
4. **مغایرت**: `expected = opening + purchases - salesConsumption - recordedWaste`; `variance = actual - expected`.
5. **ارزش مغایرت**: قیمت مبنا برای هر دوره = میانگین موزون خرید همان ماده بین شمارش قبلی/موجودی مبنا و شمارش فعلی. اگر خریدی نباشد، آخرین خرید قبل از دوره و سپس آخرین قیمت ثبت‌شده ماده استفاده می‌شود.
6. **Snapshot**: `periodUnitCostToman`, `periodPurchaseQuantity`, `periodPurchaseValueToman`, `unitCostSource`, `lossValueToman` در خط انبارگردانی جدید ذخیره می‌شوند.
7. **گزارش تجمعی**: در گزارشات، بازه 1/3/6/12 ماه برای خسارت مغایرت نامشخص، خرید همان بازه و نسبت خسارت به خرید اضافه شده است.

## تست محاسباتی
- خرید 1: 5 × 300,000 تومان با ورود 50 واحد
- خرید 2: 10 × 320,000 تومان با ورود 100 واحد
- میانگین موزون: 31,333.33 تومان به ازای واحد
- موجودی مبنا 100 + ورود 150 - مصرف فروش 90 - ضایعات 10 = موجودی مورد انتظار 150
- شمارش واقعی 140 ⇒ مغایرت نامشخص 10- واحد
- خسارت = 10 × 31,333.33 = حدود 313,333 تومان

تمام فایل‌های JavaScript با `node --check` بررسی شدند.
